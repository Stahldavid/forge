# S1.3-A — Adoption Record

**Record ID:** `AF-S1.3-A-ADOPTION-2026-09-12`

**State recorded:** `ADOPTED` on 2026-09-12; this record documents the already-completed adoption event for S1.3-A and does not create a second adoption.

## 1. Exact adoption coordinates

| Coordinate | Value |
| --- | --- |
| Repository / PR | [Stahldavid/forge #19](https://github.com/Stahldavid/forge/pull/19) |
| Adopted S1.3 planning baseline | `801c2192e64652ade19a642b2f09f8660963c74f` |
| Current repository/reproduction baseline / first merge parent | `92c470ae2319aed9bde734d4506767677abe5a3c` |
| Independently reviewed S1.3-A head / second merge parent | `b48a8fb4d77a1d8f5df7d9d523623a2037966da0` |
| Adoption merge on `main` | [`de9449648d821086db3d6f214c1a39cd4c659c7e`](https://github.com/Stahldavid/forge/commit/de9449648d821086db3d6f214c1a39cd4c659c7e) |
| Merged at | `2026-09-12T19:59:43Z` |
| Adopted tree | `8056dc4b03c0065dd45c0779eb74abf4786306b6` |

The merge has exactly the two parents listed above. The independently reviewed head and the adoption merge have the same tree, so the documentary/evidence content reviewed at `b48a8fb4…` is the content adopted on `main`.

The repository baseline moved during review because PR #20 separately adopted the Delta semantic-timeline PGlite timeout/cleanup repair. That movement was explicitly assessed under S13-A01 before the final S1.3-A review. The adopted S1.3 planning coordinate remains `801c2192…`; reproduction for the final S1.3-A candidate was bound to the repair-aware repository baseline `92c470ae…`.

## 2. What S1.3-A adopted

S1.3-A adopts the repository-native **Evidence Registry & Reproduction Baseline** for the already accepted Agent Fabric P0a/S1.1/S1.2 scope. It does not create a new normative kernel or production-readiness claim.

The adopted package consists of:

- [`S1.3_EVIDENCE_CATALOG.md`](./S1.3_EVIDENCE_CATALOG.md): stable `AF-EV-*` records with source/subject, workflow association, checkout/execution, environment, observed result, limitations and adoption coordinates where applicable;
- [`S1.3_CONFORMANCE_MATRIX.md`](./S1.3_CONFORMANCE_MATRIX.md): individual evidence disposition for all **24** adopted `OP-*` rules, all **35** adopted `I-*` invariants and all **16** persisted event families;
- [`S1.3_REPRODUCTION_PROTOCOL.md`](./S1.3_REPRODUCTION_PROTOCOL.md): exact-SHA/worktree discipline, GitHub checkout provenance, retry/timeout rules, local/environment metadata and result normalization;
- [`S1.3_GAP_AND_TOOLING_REGISTER.md`](./S1.3_GAP_AND_TOOLING_REGISTER.md): classified evidence/applicability gaps, blocking impact, closure evidence and explicit decisions that no new tooling category is yet justified by S1.3-A.

The evidence package preserves the distinction among `PASS`, `FAIL`, `SKIPPED`, `NOT_APPLICABLE`, `BLOCKED` and `INCONCLUSIVE`. A green workflow cannot erase a failing internal diagnostic, an older failed attempt or an unresolved metadata/applicability limitation.

## 3. Baseline movement and separately adopted repair

PR #20 separately adopted reviewed repair `0c8fe25d65ff5619395f3cf1f24635ba99a8e34c` through merge `92c470ae2319aed9bde734d4506767677abe5a3c` before the final S1.3-A review.

The complete delta from the original S1.3 planning/evidence baseline `801c2192…` to `92c470ae…` changes only `tests/delta/semantic-timeline.test.ts` (+22/-10): five real-PGlite integration tests receive 30-second budgets and attempt store closure in `finally`. Fixtures, assertions and the 180-artifact input remain unchanged. No Agent Fabric runtime, schema, normative rule, dependency or workflow semantics moved with that baseline.

The prior S1.3-A candidate history remains preserved. In particular:

- `AF-EV-0052` retains the exact-head CI #302 failure on `afa3f108…`, where the 180-artifact test exceeded the old 5-second budget;
- `AF-EV-0053` retains the exact-head Security #242 observation and its three internal framework failures;
- `AF-EV-0054` retains the failed clean-start local test attempt on the pre-repair candidate;
- `AF-EV-0055/0056` retain the separately scoped PR #20 CI/Security evidence.

The later successful final-candidate runs do not rewrite or delete those observations.

## 4. Exact-final-SHA technical evidence

The final reviewed S1.3-A candidate was `b48a8fb4d77a1d8f5df7d9d523623a2037966da0`, tree `8056dc4b03c0065dd45c0779eb74abf4786306b6`.

### CI exact-head execution

[CI #306, run 34714782156](https://github.com/Stahldavid/forge/actions/runs/34714782156), attempt 1, executed the exact candidate SHA directly.

A separate non-adoption verification branch used workflow-definition commit `c78ad6d5f92fe93de169f250d55d8b23ad8cc42a`. Its only workflow changes were manual dispatch plus checkout/provenance assertions pinning `b48a8fb4…`; the original CI jobs, commands, action versions, Node/OS matrix and timeouts were preserved.

All eight jobs passed their initial clean-worktree/SHA assertion and completed successfully. The main verify job reported:

```text
1330 pass
2 skip
0 fail
```

Typecheck, unit/integration, generation, compiler workspace verification, example drift, package-manager smoke, packed-package smoke, all six Node 22/24 OS jobs and external quickstart completed successfully. The repaired 180-artifact semantic-timeline case passed within its adopted 30-second budget.

### Security exact-head execution

[Security Assurance #246, run 34714750506](https://github.com/Stahldavid/forge/actions/runs/34714750506) was manually dispatched on the candidate branch and checked out `b48a8fb4d77a1d8f5df7d9d523623a2037966da0` directly.

The workflow completed `SUCCESS`; the dedicated security suite reported **32 pass / 0 fail**, and strict TestGraph completed. The workflow's internal framework-verification surface still printed failures for `generate-check`, `agent-contract-check` and `rls-check`. Those diagnostics remain **INCONCLUSIVE** for an aggregate framework-green claim and are not converted into success by the workflow conclusion.

The retained security-evidence artifact is identified in the PR evidence as artifact `10303983249`, SHA-256 `985f0fd318f7814a85725ea385c6ebfbf638966a6922a5bca9b62ba51337415b`.

### Other execution observations

- Automatically triggered PR CI/Security runs were synthetic-merge observations and were retained separately rather than misreported as exact-head execution.
- Nuxt Template Smoke was `NOT_APPLICABLE` under its existing path filter for the five-document S1.3-A diff.
- Broad local `verify framework` remained failed/variant and is preserved under the existing environment-variance evidence gap. No timeout/test/workflow modification was used to normalize that local result inside S1.3-A.

## 5. Independent review and S13-A gate completion

The final independent review of `b48a8fb4d77a1d8f5df7d9d523623a2037966da0` reported:

```text
BLOCKER 0
HIGH    0
MEDIUM  0

S13-A01  SATISFIED
S13-A02…A11  SATISFIED
S13-A12 repository checks  SATISFIED
S13-A12 independent review SATISFIED

READY_FOR_MERGE_REVIEW
```

The user then explicitly authorized adoption/merge. PR #19 was marked ready and merged by merge commit `de9449648…`, completing S13-A12.

The adopted gate disposition is therefore:

| Gate | Adoption disposition |
| --- | --- |
| S13-A01 | complete — planning baseline and repair-aware repository/reproduction baseline identified; PR #20 movement assessed |
| S13-A02 | complete — all four required S1.3-A artifacts exist with stable evidence/gap IDs |
| S13-A03 | complete — 24 OP / 35 I / 16 event entries individually represented |
| S13-A04 | complete — executed evidence separates subject, workflow association, checkout, execution, tree equivalence and adoption coordinates |
| S13-A05 | complete — historical acceptance, current reproduction, source mapping and structural evidence remain distinct |
| S13-A06 | complete — FAIL/SKIPPED/NOT_APPLICABLE/BLOCKED/INCONCLUSIVE semantics preserved; historical failures not normalized away |
| S13-A07 | complete — reproduction procedures exist; unrecoverable metadata is explicitly gapped |
| S13-A08 | complete — all current gaps have class, affected scope, blocking impact and minimum closure evidence |
| S13-A09 | complete — no unclassified applicable gap and no current OP/I `BLOCKING_GAP` in the adopted inventory |
| S13-A10 | complete — tooling categories remain proposals/decisions only; none implemented by S1.3-A |
| S13-A11 | complete — adopted S1.3-A diff is documentary/evidence-organization only |
| S13-A12 | complete — exact-final-SHA applicable repository checks, independent review with zero unresolved BLOCKER/HIGH/MEDIUM and explicitly authorized merge |

## 6. Preserved gaps and applicability boundaries

S1.3-A adoption establishes a truthful evidence baseline; it does **not** mean the evidence corpus is exhaustive.

The following adopted gap states remain intentionally visible after adoption:

- `S13-GAP-001` — direct own-key special-identifier vector for `createDispatchOffer` is still missing; `OP-DSP-002` remains partial only in that evidence dimension;
- `S13-GAP-002` — local broad framework verification remains environment-variant/inconclusive and cannot be summarized as deterministically green;
- `S13-GAP-008` — historical local-framework evidence lacks the full exact metadata tuple required by the S1.3 reproduction protocol;
- `S13-GAP-009` — several historical GitHub workflow records lack independently established checkout/execution coordinates and remain `CHECKOUT_NOT_ESTABLISHED` rather than inferred;
- `S13-GAP-003` through `007` preserve adopted `ASSUMPTION`, `STRUCTURAL_ONLY` and `DIRECTION_ONLY` applicability boundaries rather than defects hidden by the catalog.

`S13-F01` remains historical review evidence: the earlier workflow-provenance conflation was a valid MEDIUM finding on an earlier candidate, was materially remediated, and is not erased by final adoption.

## 7. Historical wording and precedence

The four adopted S1.3-A source artifacts retain `IMPLEMENTATION_CANDIDATE`, pending-review and pre-adoption wording from the exact content that passed independent review. Those labels are intentionally retained for traceability rather than rewritten after the fact. This adoption record and merge `de9449648…` establish the subsequent `ADOPTED` state.

S1.3-A remains evidence/reproduction metadata over the accepted architecture. It cannot override S1.1 normative operation/invariant obligations, turn S1.2 implementation mappings into new authority, or silently supersede the frozen P0a behavior.

## 8. What this adoption does not authorize

S1.3-A adoption does **not** automatically authorize:

- a generic conformance runner;
- formal state-machine/model checking;
- proof/evidence signing;
- cross-language/JCS conformance;
- a machine-readable evidence generator/manifest;
- a production evidence-retention service;
- runtime, schema, dependency or workflow changes;
- blanket timeout increases or test rewrites;
- P0b, provider execution, production persistence/outbox, consequential effects, reconciliation, recovery epochs, routing or production-readiness certification.

Any gap closure or tooling proposal after S1.3-A requires its own objective, narrow scope, exact-SHA evidence, independent review and explicitly authorized adoption. In particular, the adopted gap register recommends a separately scoped test-only closure for `S13-GAP-001`, controlled reproduction before proposing changes for `S13-GAP-002/008`, and evidence-only recovery for `S13-GAP-009`.